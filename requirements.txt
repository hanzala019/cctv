# Runtime dependencies.
#
# Pinned exactly so everyone on the team resolves the same versions.
# Bump deliberately, in its own commit, with a note on what you tested.
#
# Verified against PyPI on 2026-08-23: all four resolve and import
# cleanly together on Python 3.12 and 3.13.

numpy==2.5.2
onnxruntime==1.29.0
PyQt6==6.11.0
psutil==7.2.2
# STAY ON 4.x. opencv-python 5.x exists and pip will happily take it if
# this is ever loosened to a range. OpenCV 5 is a major version with API
# changes we have not tested against -- especially VideoCapture backend
# behaviour and the VideoWriter fourcc fallbacks in
# cctv/recording/recording_manager.py.
opencv-python==4.14.0.94
